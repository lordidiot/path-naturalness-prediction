To create a local sqlite database, do the following:

1. Download a CSV file from [ConceptNet Downloads](https://github.com/commonsense/conceptnet5/wiki/Downloads). Put the file `assertions.csv` to this folder.
2. From the parent folder (root directory of this repository), run `python data/csv_to_python.py`. This will generate a local database `assertions.sqlite`.
